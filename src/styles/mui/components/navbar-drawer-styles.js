import { primary } from '@/styles/common/colors';

export const muiDrawerStyles = { '& .MuiPaper-root': { backgroundColor: primary } };

export const drawerContainerBoxStyles = {
  width: 250,
  background: primary,

};

export const closeIconButtonStyles = { cursor: 'pointer', zIndex: 4 };
